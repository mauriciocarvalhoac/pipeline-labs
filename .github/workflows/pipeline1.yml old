name: Pipeline de teste

on:
  push:
    branches: [ master ]

jobs:
  hello-jobs:
    name: "Hello World"
    runs-on: ubuntu-latest

    steps:
      - name: checkout no código
        uses: actions/checkout@v4

      - name: Executar o hello World
        run: echo "Hello Github Actions"

      - name: Variáveis de Ambiente
        run: |
          echo "Sistema operacional ${{runner.os}}"
          sleep 1
          echo "Diretório Atual: ${pwd}"
          sleep 2
          echo "Data Atual: ${date}"
          sleep 1
          echo "Usuário: ${whoami}"

      - name: Listar arquivos do repositório
        run: ls -la

      - name: Vários comandos
        run: |
          echo "Este é o Lab 01"
          echo "Executado com sucesso"
          echo "Pronto para os próximos desafios"
